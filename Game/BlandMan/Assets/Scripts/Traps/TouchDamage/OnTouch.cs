using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnTouch : MonoBehaviour
{

    // Public variables
    // Damage received by player when touching this object
    public int touchDmg = 1;
    // Magnitude of knockback in X direction for the player when touching this object
    public int forcedMovementX = 1;
    // Magnitude of knockback in y direction for the player when touching this object
    public int forcedMovementY = 0;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
